apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: accounts
  labels:
    app: accounts
spec:
  to:
    kind: Service
    name: accounts
    weight: 100
  port:
    targetPort: 8080
  wildcardPolicy: None
